# 📊 Статистика активности пользователей

Команда `/stats` позволяет администраторам просматривать статистику активности пользователей в виде двух наглядных графиков.

## Использование

### Статистика конкретного пользователя

Чтобы получить статистику конкретного пользователя:

1. В DEBUG чате найдите сообщение с меткой `USER123456789` (где 123456789 - это Telegram ID пользователя)
2. Ответьте на это сообщение командой `/stats`
3. Бот автоматически определит ID пользователя из метки и сгенерирует статистику

```
USER123456789        <- Ответьте на это сообщение
[пересланное сообщение]

Ваш ответ: /stats
```

### Статистика всех пользователей

Чтобы получить общую статистику по всем пользователям:

1. Просто отправьте команду `/stats` без ответа на сообщение

```
/stats
```

## Что показывает статистика

Команда генерирует два графика:

### 1. Активность по часам суток

Показывает распределение сообщений по 24 часам суток:
- Ось X: часы (0-23)
- Ось Y: количество сообщений
- Пиковые часы подсвечиваются оранжевым цветом

### 2. Активность по дням недели

Показывает распределение сообщений по дням недели:
- Ось X: дни недели (Понедельник - Воскресенье)
- Ось Y: количество сообщений
- Самый активный день подсвечивается золотым цветом

## Технические детали

- Статистика основана на всех timestamps, сохраненных в базе данных для указанного пользователя
- Timestamps берутся из поля `prompt` каждого сообщения в БД
- Графики генерируются с помощью библиотеки `matplotlib`
- Изображения отправляются в формате PNG с высоким разрешением (150 DPI)

## Примеры использования

### Пример 1: Анализ активности конкретного пользователя

Администратор может проанализировать, в какое время суток пользователь наиболее активен, чтобы оптимизировать время отправки напоминаний.

### Пример 2: Общий анализ аудитории

Просмотр общей статистики помогает понять:
- В какие часы пользователи наиболее активны
- Какие дни недели самые загруженные
- Стоит ли менять временные интервалы для отправки напоминаний

## Требования

Для работы команды требуется:
- Права администратора (USER ID должен быть в `ADMIN_LIST`)
- Установленная библиотека `matplotlib` (включена в `requirements.txt`)
- Наличие данных в базе (пользователь должен был отправить хотя бы одно сообщение)

## Обработка ошибок

Если данных нет или пользователь не найден, команда выведет сообщение:
```
❌ Нет данных для отображения статистики. 
Возможно, пользователь не отправлял сообщений.
```

При других ошибках команда выведет:
```
❌ Ошибка при генерации статистики: [описание ошибки]
```

## Производительность

- Генерация статистики обычно занимает 1-3 секунды
- Время зависит от количества сообщений в базе данных
- Во время генерации отображается сообщение "⏳ Собираю статистику..."

